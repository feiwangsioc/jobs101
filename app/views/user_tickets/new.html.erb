<h1>New Ticket</h1>

<%= form_for [@user, @ticket] do |f| %>

  <%= render :partial => "form", :locals => { :f => f } %>

  <div class="form-group">
    <%= f.submit "Create", :class => "btn btn-primary" %>
    <%= link_to "Cancel", user_tickets_path(@user) %>
  </div>
<% end %>